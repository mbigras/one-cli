# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'one', :github => 'mbigras/one'

group :test do
  gem 'aruba', '~> 0.14.0'
end